#include <stdio.h>

typedef struct
{
	char url[101];
	int relevance;
}
page;

int main()
{
	page cases[10];
	int N;

	while(N>0)
	{
		for(i=0;i<10;i++)
		{
			scanf("%s %d", &cases[i].url, &cases[i].relevance);

			while();

		N--;
	}